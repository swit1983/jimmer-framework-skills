# jimmer-framework

Jimmer 框架开发帮助 Skill，包含完整官方文档整理、分类 API 说明和代码示例。

**当以下情况时使用本 Skill**：
- 用户提到 "jimmer 框架" / "jimmer orm"
- 用户询问 "jimmer 使用" / "jimmer 查询" / "jimmer 保存"
- 用户要求 "帮我写 jimmer 代码" / "jimmer 示例"
- 用户提到 "jimmer 注解" / "jimmer api"

本 Skill 基于 Jimmer 官方中文文档 https://jimmer.deno.dev/zh/docs/ 整理。

---

## 📁 目录结构

```
jimmer-framework/
├── SKILL.md              # 本文件
├── extract-docs.py       # 文档下载脚本（从官网下载全部文档）
├── docs/                 # 官方文档按分类整理
│   ├── cache/            # 缓存 (9篇)
│   ├── client/           # 客户端 (2篇)
│   ├── configuration/     # 配置 (16篇)
│   ├── graphql/          # GraphQL (3篇)
│   ├── mapping/          # ✅ 实体映射 (25篇) - 核心
│   ├── mutation/         # ✅ 修改保存 (20篇) - 核心
│   ├── object/           # ✅ 对象特性 (10篇) - 核心
│   ├── overview/         # 框架介绍 (4篇)
│   ├── query/            # ✅ 查询 (31篇) - 核心
│   ├── quick-view/       # 快速预览 (21篇)
│   ├── resource/         # 生态资源 (3篇)
│   └── spring/           # Spring集成 (7篇)
└── examples/             # 案例代码，原 showcase 按分类整理
    ├── mapping/          # 映射示例（预留）
    ├── query/            # 查询案例
    │   ├── object-fetcher/  # 对象抓取器案例
    │   └── paging/         # 分页案例
    ├── mutation/         # 修改案例
    ├── dto/              # DTO转换示例（预留）
    ├── object/           # 对象特性示例（预留）
    ├── cache/            # 缓存示例（预留）
    ├── configuration/     # 配置示例（预留）
    ├── spring/           # Spring集成示例（预留）
    └── showcase/         # 未分类案例
```

---

## 🎯 Jimmer 框架核心特色

| 特性 | 说明 | 难度 |
|------|------|------|
| **任意形状查询** | 对象抓取器按需抓取，不需要提前定义固定 DTO | ⭐⭐⭐ |
| **一次保存任意形状** | 根对象保存自动级联处理所有关联 | ⭐⭐⭐⭐ |
| **智能动态 JOIN** | 自动解决 N+1 问题，支持弱连接、动态优化 | ⭐⭐⭐⭐⭐ |
| **三级缓存体系** | 对象缓存 + 关联缓存 + 计算缓存，自动维护一致性 | ⭐⭐⭐⭐⭐ |
| **不可变性设计** | Draft 模式实现优雅修改 | ⭐⭐⭐⭐ |
| **原生 DTO 支持** | 内置 DTO 语言，无需手动转换 | ⭐⭐⭐ |

---

## 🗺️ 核心模块详细说明

### 1. 🗺️ **mapping - 实体映射** ✅ **重点**

#### 基础映射

| 内容 | 说明 | 重点 |
|------|------|------|
| 可空性 | 理解 `null`/`optional`/`required` | |
| 简单映射 | 基础类型映射配置 | |
| 关联映射 | 一对一/多对一/一对多/多对多 | ⚠️ **多对多需要重点注意中间表配置** |
| 命名策略 | 表名/字段名转换 | |
| 真假外键 | 物理外键 vs 逻辑外键 | |
| JSON Converter | JSON 类型映射 | |
| 更多类型 | 支持的其他类型 | |

#### 进阶映射 ⚠️ **易错内容详细说明**

| 内容 | 说明 | 重点 |
|------|------|------|
| 复合字段 | `@Embedded` 嵌入式字段 | |
| MappedSuperclass | 继承映射 | |
| **逻辑删除** | 实体表/中间表两种方式处理 | ⚠️ 注意中间表逻辑删除配置不同 |
| **视图属性** | `IdView`/`ManyToManyView` | ⭐ 特色功能，减少DTO编写 |
| **计算属性** | `formula` 简单计算 / `transient` 复杂计算 | ⚠️ **注意两种方式区别** |
| Enum 映射 | 枚举类型配置 | |
| JSON 映射 | JSON 字段映射 | |
| 非结构化映射 | 自定义 JOIN SQL | |
| 中间表过滤器 | 多对多关系过滤 | ⭐ 特色功能 |
| Key | 主键配置 | |
| 远程关联 | 跨服务关联 | |
| **OnDissociate** | 脱钩操作处理 | ⚠️ **脱钩易错，需要注意配置** |

---

### 2. 🔍 **query - 查询** ✅ **重点**

| 模块 | 说明 | 重点 |
|------|------|------|
| **对象抓取器** | `Fetcher` 按需抓取任意形状 | ⭐ 核心特色 |
| - 基本用法 | 基础抓取语法 | |
| - 普通属性 | 简单属性选择 | |
| - **关联属性** | 关联对象抓取 | ⚠️ 多级关联注意 |
| - **递归查询** | 递归树形结构 | ⚠️ **注意深度限制和节点控制** |
| - 视图属性 | 结合视图属性使用 | |
| - DTO 查询 | 直接抓取到 DTO | |
| - 和 Spring Data 协同 | 与 Spring Data 集成 | |
| **动态筛选** | 动态条件查询 | |
| **动态 JOIN** | 解决 N+1 问题 | ⭐⭐⭐⭐ 核心特色，重点理解 |
| - 问题分析 | 其他方案的问题 | |
| - 链式关联路径 | | |
| - 合并冲突连接 | | |
| - 优化不必要连接 | | |
| - 分页安全性 | | |
| - **弱连接** | 可选连接，不强制内连接 | ⭐ 特色 |
| - Kotlin 特有 | Kotlin 语法糖 | |
| **隐式子查询** | 隐式子查询支持 | |
| **动态排序** | 动态排序配置 | |
| **聚合分组** | 聚合查询 | |
| **智能分页** | 各种分页优化 | ⭐ 特色 |
| - 基本用法 | | |
| - 表连接优化 | | |
| - 反排序优化 | | |
| - **深分页优化** | ⭐ 深度分页优化技巧 | |
| DSL 表达式 | 基础 DSL 语法 | |
| Native 表达式 | 原生 SQL 嵌入 | |
| Base-Query | 基础查询抽象 | |
| **全局过滤器** | 全局过滤条件 | ⭐ 特色 |
| - 自定义过滤器 | | |
| - **逻辑删除** | 全局处理逻辑删除 | |
| **子查询** | 子查询支持 | |
| **直接查询中间表** | 直接查询关联表 | |
| **超级 QBE** | 示例查询 | |

---

### 3. ✏️ **mutation - 修改保存** ✅ **重点**

| 模块 | 说明 | 重点 |
|------|------|------|
| 修改语句 | 基础 update/delete | |
| 删除语句 | 基础删除 | |
| **保存指令** | `save` 指令是核心 | ⭐⭐⭐⭐ 核心 |
| - 基本用法 | | |
| - 数据分类 | 分辩 standing/detached/transient | ⚠️ **易错** |
| - **根对象保存模式** | `append`/`merge`/`delete` | ⚠️ **必须理解三种模式区别** |
| - **关联对象** | 关联保存 | ⭐⭐⭐⭐ **最复杂最重要** |
|   - 关联分类 | 不同分类处理不同 | ⚠️ |
|   - 拥有方特有功能 | 只有拥有方可操作 | ⚠️ |
|   - 关联对象保存模式 | | |
|   - **脱钩操作** | 解除关联 `dissociate` | ⚠️ **易错，必须注意** |
| - 违反约束异常处理 | | |
| - **乐观锁 / 悲观锁** | 两种锁用法 | ⚠️ 注意区别 |
| - MySQL 特殊问题 | | |
| - **Input DTO** | DTO 直接保存 | ⭐ 特色 |
|   - 问题分析 | 传统方式的问题 | |
|   - 孤单表单问题 | | |
|   - **DTO 语言** | Jimmer 原生 DTO | ⭐ 特色 |
|   - **空值处理** | `null`/不设置 区别 | ⚠️ **非常易错** |
|   - MapStruct 集成 | | |
| 删除指令 | 独立删除指令 | |
| 直接修改关联 | 直接修改中间表 | |
| 保存前拦截器 | Draft 拦截 | |
| 触发器 | 保存触发器 | |

---

### 4. 🎭 **object - 对象特性** ✅ **重点**

| 内容 | 说明 | 重点 |
|------|------|------|
| 动态性 | 动态对象支持 | |
| **不可变性** | 设计理念 | ⭐ 核心设计 |
| - 原因 | 为什么需要不可变性 | |
| - 现状 | 其他框架现状 | |
| - 方案 | Jimmer 的解决方案 | |
| **Draft** | Draft 模式 | ⭐ 实现不可变性下的优雅修改 |
| Jackson 集成 | 和 Jackson 协同 | |
| **DTO 转换** | 原生 DTO 语言 | ⭐ 特色 |
| - DTO 语言 | Jimmer 原生支持 | |
| - MapStruct 集成 | 与 MapStruct 兼容 | |
| 可见性 | 属性可见性控制 | |
| 工具方法 | 常用工具 | |

---

### 5. 📦 **cache - 缓存**

| 内容 | 说明 | 重点 |
|------|------|------|
| 启用缓存 | 基础配置 | |
| **缓存类型** | | ⭐ 三级缓存体系 |
| - 对象缓存 | 实体对象缓存 | |
| - 关联缓存 | 关联集合缓存 | |
| - 计算缓存 | 计算结果缓存 | |
| **缓存一致性** | 自动维护一致性 | ⭐ 特色，自动保证 |
| **多视角缓存** | 不同用户不同缓存 | ⭐ 高级特色 |
| - 基本概念 | | |
| - 自定义过滤器 | | |
| - 高级用法 | | |
| - 缓存未生效排查 | ⚠️ 常见问题 |

---

### 6. ⚙️ **configuration - 配置**

常用配置项简要说明：

| 配置 | 说明 |
|------|------|
| dialect | 数据库方言 |
| connection-manager | 连接管理 |
| multi-datasources | 多数据源 |
| batch-size | 批量操作大小 |
| sql-log | SQL 日志 |
| default-enum-strategy | 默认枚举策略 |
| default-database-stragegy | 默认命名策略 |
| trigger-type | 触发器类型 |
| database-validation | 数据库验证 |
| micro-service | 微服务 |
| cache-abandoned | 缓存未生效排查 |
| id-only-target-checking-level | 短关联 ID 检查 |
| dissociate-action-checking | 脱钩操作检查 |
| in-list-optimization | IN 列表优化 |

---

### 7. 🔹 **其他模块**

| 模块 | 说明 |
|------|------|
| `overview/` | 框架介绍、核心特性、项目入门 |
| `quick-view/` | 快速预览、快速上手教程（新手入门） |
| `client/` | 客户端 API 生成 |
| `spring/` | Spring / Spring Cloud 集成 |
| `graphql/` | GraphQL 集成 |
| `resource/` | 生态资源 |

---

## ⚠️ 常见易错点汇总

这里列出最容易出错的地方，使用 Jimmer 特别注意：

1. **脱钩操作 (dissociate)** - 理解何时可以脱钩，拥有方和逆方的区别
2. **空值处理** - `null` 和 不传递的区别，对于 DTO 保存
3. **多对多** - 中间表配置，谁是拥有方
4. **递归查询** - 深度控制，循环引用处理
5. **计算属性** - `formula` vs `transient` 选择
6. **根对象保存模式** - `append`/`merge`/`delete` 三种模式区别
7. **关联保存** - 拥有方才能修改关联
8. **逻辑删除** - 实体表 vs 中间表配置不同
9. **动态 JOIN** - 强连接 vs 弱连接区别
10. **深分页** - 优化技巧使用场景

---

## 💡 使用方式

当用户提问关于 Jimmer 的问题时：

1. 使用 `knowledgebase-retrieve` 搜索相关文档内容
2. 根据问题定位到具体模块
3. **对于核心特色/易错内容**，从文档提取详细说明
4. **对于简单 API**，给出简要说明即可
5. 结合 examples 中的案例给出代码示例
6. 必要时提醒用户注意易错点

---

## 📊 统计

- 总文档：**175** 篇
  - `docs/`: **151** 篇（按分类整理）
  - `examples/`: **24** 篇（案例按分类整理）
- 总大小：**2.2 MB**
- 核心模块文档：**102** 篇（mapping + query + mutation + object）

> 原 showcase 案例已全部按分类整理到 examples 对应目录
